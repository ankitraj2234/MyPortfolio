import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ankit Raj | Solutions Engineer & Developer",
  description: "Portfolio of Ankit Raj - Solutions Engineer & Developer specializing in secure software development, Android apps, and enterprise solutions.",
  keywords: ["Ankit Raj", "Solutions Engineer", "Developer", "SecureDev", "Portfolio", "Android Developer"],
  authors: [{ name: "Ankit Raj" }],
  icons: {
    icon: "/favicon.svg",
    apple: "/apple-icon.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
